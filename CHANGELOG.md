# 更新日志

## [2.0.0] — 2026-09-17

第一个正式版本。把 v1.0 那个“改代码 → 运行 → 打印一个状态码”的手动脚本，
重写成可以直接双击使用的 Windows 桌面工具。

### 新增

* **图形界面**：账号 / 密码 / 运营商（移动 / 联通 / 电信 / 校园网不代拨）输入框，
  一键连接、断开，实时显示连接状态与在线时长。
* **记住账号密码**：密码用 Windows DPAPI（当前用户作用域）加密后存入
  `%APPDATA%\CampusNetPortal\secret.bin`，换机器或换 Windows 用户都无法解密；
  取消勾选会立刻删除该文件。
* **开机静默自启**：写入 `HKCU\...\CurrentVersion\Run`（**不需要管理员权限**），
  开机后不显示窗口、不占任务栏，只在右下角托盘出现一个小图标并后台自动认证。
* **系统托盘**：左键打开主界面，右键菜单可打开界面 / 立即重连 / 开关自启 / 退出。
  点窗口的 × 只是隐藏，程序继续在后台保持认证。
* **断线自动重连**：认证失败按 5→10→20→40→60 秒指数退避，不会疯狂重试。
* **防掉线保活**：见下。
* **单实例运行**：重复启动会直接退出，不会两个进程互相抢认证。
* **日志**：`%APPDATA%\CampusNetPortal\app.log`，超过 128 KB 自动截断。

### 防掉线保活的实现

* 用 `GetIfTable2`（Windows IP Helper API，**只读、几乎零开销**）读取真实网卡
  的累计收发字节数，判断“最近是不是真的没有流量”。
  按 `InterfaceAndOperStatusFlags` 的 `IF_HARDWARE` 位只取物理网卡，
  并排除与它共用同一组计数的 NDIS 轻量筛选器。
* **正常上网时一个字节都不额外发**；只有连续空闲超过阈值（默认 150 秒）
  才补发一次几十字节的极小 HTTP 请求，刷新 NAT / portal 的活跃计时。
* 每 45 秒 GET 一次 `userstatus.jsp` 检查登录状态，被踢下线立刻重新认证。

### 实测修正

在真实 portal 上联调后修正了几处想当然的实现：

* **认证判据**：portal **会校验密码**——错误密码会返回登录页并提示“登录失败”，
  账号不存在也一样，只有密码正确才会返回 `userstatus.jsp` 并显示
  `当前登录账号：<账号>@<运营商>`。因此成功判据是“响应里出现与本账号一致的
  登录账号”，**不能**用“外网通不通”判断（认证成功前外部探测往往全是失败的）。
* **页面编码是 GB2312**，原先按 UTF-8 解码会把中文变成乱码，已按响应头解码。
* **网卡流量统计曾算错 6 倍**：NDIS 轻量筛选器与真实网卡共用同一组计数器，
  未按硬件标志过滤会把同一份流量重复累加；`MIB_IF_ROW2` 结构体尺寸也曾多出
  8 字节导致多网卡时读取错位。两者都已修正并加了断言测试。

### 认证失败时的提示

被 portal 拒绝时会翻译成人话，而不是丢一个状态码：
“密码错误 / 账号不存在 / 账号已欠费停机 / 登录失败”等。

### 其它

* 托盘图标、程序图标均用标准库 + ctypes 自行实现（ICO 由代码按格式生成），
  因此除了 `requests` 之外**没有任何第三方依赖**，没有 pystray / pywin32 / Pillow。
* 打包为单文件 exe，约 12 MB，双击即用，不需要 Python 环境。
* 空闲时 CPU 占用 ≈ 0%，内存约 9–30 MB。

## [1.0.0] — 原始脚本

`net.py`：账号密码硬编码在源码里，用 `requests.post` 依次请求 logoff 和 login，
最后打印一个 HTTP 状态码。需要自己改代码、自己运行。
