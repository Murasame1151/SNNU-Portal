# SNNU-Portal v2.2 · 新增定时重连 / 唤醒重连 / 手动断开不自动重连

在 v2.1（Windows + Linux 双平台）基础上，新增重连控制。

## 下载

| 平台 | 文件 | 说明 |
| --- | --- | --- |
| Windows | `CampusNetAutoLogin-v2.2.exe` | 12 MB 单文件，双击即用，免装 Python |
| Linux | `SNNU-Portal-v2.2-linux-x86_64` | 13 MB 单文件 ELF，免装 Python |
| Linux | `SNNU-Portal-2.2-linux-x86_64.tar.gz` | 上面这个 + 图标 + 安装脚本 + 使用说明 |
| 源码 | Source code (zip/tar.gz) | 自己构建 |

Linux 运行前装一下 Tk（界面需要，程序本体已自带 Python 与 requests）：

```bash
sudo apt install python3-tk
./SNNU-Portal-v2.2-linux-x86_64
```

## 本次新增

**1. 定时重连**
设置里填 `HH:MM`（例如 `02:00`），每天到这个时刻**先注销再重新认证**一次，
拿到的是全新会话。**留空则不启用**。按日期去重，同一天只执行一次。

**2. 睡眠唤醒后立即重连**
从睡眠 / 休眠恢复后立刻重新认证一次。靠主循环的时间跳变检测（循环每 5 秒一轮，
若发现墙上时钟一次跳了 120 秒以上即判定为睡眠归来）。可在设置里关闭。

**3. 探活周期最小值 30 秒 → 5 秒**
想要更密集地确认在线状态时可以调到 5 秒；默认仍是 45 秒。

**4. 手动断开后不再自动重连**
点“断开”后程序进入手动断开状态：不再自动登录、不因探活失败而重连，
并且写入配置（`want_online=0`）——**关掉程序再打开也不会自己连上**。
点一次“连接”，或等到定时 / 唤醒事件触发，才恢复自动重连。

## 新增配置项

| 键 | 默认 | 含义 |
| --- | --- | --- |
| `daily_reconnect` | 空 | 每天定时重连，格式 `HH:MM`；留空不启用 |
| `wake_reconnect` | `1` | 睡眠唤醒后立即重连 |
| `want_online` | `1` | 是否希望在线；手动断开后置 0 |

## 验证情况

* 编译检查通过（Windows 与 Debian 13 双侧 `py_compile`）。
* **按你的要求没有做联网试运行**，因此没有跑会触发认证的端到端测试；
  离线自检 `logictest.py` 也未执行（其中包含注册表写入等本机副作用）。
* 两个平台的二进制均为本次源码重新构建，`--version` 输出为 `v2.2`。

## 已知限制

* 定时重连按分钟匹配：程序若在该分钟内恰好没在运行，则当天不会补跑。
* Linux 二进制为 **x86_64**，需 glibc ≥ 2.36（Debian 12+ / Ubuntu 22.04+），
  运行界面需要系统有 Tk。
* 仅支持陕西师范大学（SNNU）的 portal 接口。

**完整说明见 [README.md](README.md)，版本变更见 [CHANGELOG.md](CHANGELOG.md)。**
